server:
  port: 8080


spring:
  datasource:
    url: jdbc:mysql://localhost:3306/charging_station?useSSL=false&serverTimezone=UTC
    username: root
    password: password
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  application:
    name: ChargingSystem

jwt:
  secret: chargingStationSecretKey
  expiration: 864000  # 十天，单位秒
